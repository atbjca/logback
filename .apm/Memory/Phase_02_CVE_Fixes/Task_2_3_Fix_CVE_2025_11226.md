---
agent: Agent_CodeFix
task_ref: Task 2.3
status: Completed
ad_hoc_delegation: false
compatibility_issues: false
important_findings: false
---

# Task Log: Task 2.3 - 修复 CVE-2025-11226（`<if>` 条件 ACE）

## Summary
Added `new` operator check to `IfAction.begin()` to prevent arbitrary code execution via `<if>` condition attributes in logback configuration files. Commit: `ac2ae32a1`.

## Details
- Followed official 1.3.x fix (commit `e3aa0f440`) adapted to 1.2.x `IfAction` (SAX-based action vs 1.3.x `IfModelHandler` model-based processor)
- Added `NEW_OPERATOR_DISALLOWED_MSG` and `NEW_OPERATOR_DISALLOWED_SEE` constants
- Added `hasNew(String)` private method that checks for `"new "` substring
- Inserted check after `OptionHelper.substVars()` (variable substitution) but before `pesb.build()` (Janino compilation)
- When `new` operator is detected, logs error messages and returns early without evaluating the condition
- No structural surprises — `IfAction.begin()` flow matched expectations from the research report

## Output
- **Modified file:** `logback-core/src/main/java/ch/qos/logback/core/joran/conditional/IfAction.java` (+15 lines)
  - Added 2 public static final String constants (lines 36-37)
  - Added `hasNew()` check block in `begin()` (lines 66-71)
  - Added `hasNew()` private method (lines 149-151)
- **Commit:** `ac2ae32a1` on `branch_1.2.x-bjca-patch`

## Issues
None

## Next Steps
Proceed to Task 2.4 — fix CVE-2026-1225 (unrestricted class instantiation in configuration)
